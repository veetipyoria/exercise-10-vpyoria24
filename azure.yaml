# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: exercise-10-vpyoria24
metadata:
  template: vite-react-appservice@0.0.1

services:
  web:
    project: .
    language: ts
    host: appservice